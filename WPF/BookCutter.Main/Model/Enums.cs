namespace BookCutter.Main
{
    /// <summary>
    /// Color scale to select from
    /// </summary>
    public enum ColorScale
    {
        Gray,
        Red,
        Green,
        Blue
    }

    public enum PhotoModeLoad
    {
        Single,
        Multiple
    }
}
